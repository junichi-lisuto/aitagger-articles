# aitagger-tools-site

**AIタッガーをご契約中のお客様向けの、お役立ちツール公開サイト。**
AIタッガー本体では扱わない周辺作業（CSV加工・他ツール連携・画像変換）をブラウザで終わらせるツールと、
その使い方、AI検索まわりの情報を置く。

`dev\gas-sales-console`（社内向けSales Console）とは**別プロジェクト**。
読み手が社外のお客様であり、顧客情報を置かない規律が正反対なので同居させない。

---

## 最重要: 顧客情報を置かない

このリポジトリの中身は**最終的に社外へ公開される**。

- **企業名・担当者名・金額・ストアコードを、コードにもモックアップにも置かない**
- 例が要るときは「A社」「sample-shop」のような仮名にする
- スクリーンショットを貼るときは、実データが写っていないか必ず確認する

`gas-sales-console` は顧客情報を扱う前提のリポジトリだが、**ここは逆**。

---

## 決まっていること

### 公開範囲（2026-09-11 決定）

**「lisuto.co.jpのAIタッガーページからだけリンクを張り、他からのアクセスを防ぐ」という案は却下した。**
リンクを張らないことはアクセス制御ではない。URLを知っていれば誰でも開けるし、必ず知られる
（Slackやメールへの貼り付け・ブラウザ拡張・Googleのクロール）。`noindex` は検索に出さないだけで、
アクセスは防げない。

ここに載るのは自社ノウハウの中でも持ち出されて困る部分（ツール・連携方法・AI検索の知見）なので、
公開状態だと競合が商談を経ずに手の内を読める。

**採用方針: 共通パスワード1つ**（Basic認証 / Netlify・Cloudflareのパスワード保護）。
契約時とメールで合言葉を渡す。個社データが1文字も載らない限り1人1アカウントは過剰。
将来「この会社にはこのツールだけ出す」が必要になった時点でSSOへ移ればよく、
**そのときコンテンツの作り直しは発生しない**（置き場所とガードだけが変わる）。

### 置き場所

**`tools.lisuto.co.jp`（別サブドメイン）。** コーポレートサイトのCMSの中には作らない。

1. 更新頻度が10倍違う（コーポレートは年数回、ここはツールが増えるたび）。同居させると
   片方の更新がもう片方のレビュー待ちで止まる
2. パスワード保護をサイト単位でかけられる。CMSの1ディレクトリだけ保護するのは面倒
3. AIタッガーのページからのリンクは、サブドメインでも何も変わらない

### すべてブラウザで動かす（2026-09-11 矢頭さん指示）

**配布型（.pyやexeを配る）にしない。すべてブラウザ上で完結させるのがベスト。**

これまでサポート担当がPythonスクリプトをメールで個別配布していたが、
それは**このページが無かったから仕方なくやっていた**ことであり、要件ではない。

- ファイルはブラウザ内だけで処理し、サーバーへ送信しない（この一文は各ツールページに明記する）
- Pythonで書かれた既存ツールも、移植可能かをまず検討する。
  `zipfile`→JSZip、`openpyxl`→SheetJS、CP932判別→`TextDecoder('shift_jis')` で概ね置き換えられる

### 情報設計

**「ツール」と「使い方」を別セクションにしない。1ツール = 1ページで、
そのページの中に「何ができるか・使い方・実行」が全部ある。**
分けると「ツールを見つける→マニュアルへ移動→該当ツールを探し直す」の往復が発生する。
マニュアルはツールの子であって兄弟ではない。

トップページの構成:
1. ヒーロー（見出し＋1行）
2. カテゴリタブ → ツールのカードグリッド
3. AI検索を知る（記事リンク集）
4. 困ったときは（サポート窓口）

**検索窓は置かない**（2026-09-11 却下）。8本程度ならカテゴリタブで足りる。
iLovePDFも検索窓を持たず7カテゴリのタブだけ。数十本になったら再検討する。

---

## デザインの作法

### 何を継ぎ、何を継がないか

AIタッガー管理画面は「道具」、このサイトは「読み物＋道具」。目的が違うので継ぐものを分ける。

**管理画面から継ぐもの:**
- 白地・ミニマル
- **青のアクセント `#2563EB`**（管理画面の「+追加」「オートタグ」ボタンと同じ青）。**新しい青を作らない**
- AIタッガーのロゴを左上に置く（「AIタッガーの一部だ」と0.5秒で伝える唯一の要素）
- 角丸・ボタンの形・線の細さ

**継がないもの:**
- **左のメニューパネル。** サイドバーは機能が固定数あって行き来する道具の作法。
  ここはツールが増え続けるので上部の水平ナビ＋カードグリッドにする
- **テーブル（表）。** 読み物の表は読まれない。カードにする
- **情報密度。** 管理画面は1画面に詰める設計。ここは余白を倍にして本文16px以上・行間1.8
- **コーポレートサイトのグリーン。** 読者はAIタッガーのユーザーなので青で通す

一言で: **色とロゴはAIタッガー管理画面から、余白と文字サイズは lisuto.co.jp から。**

### 色（この5つ以外を増やさない）

| 用途 | 値 |
|---|---|
| アクセント | `#2563EB` |
| 見出し | `#1E3A8A` |
| 本文 | `#0F172A` / 補助 `#475569` / 薄 `#64748B` |
| 罫線 | `#E2E8F0` / 面の色 `#F8FAFC` |
| OK | `#16A34A` / 注意 `#B45309` |

カテゴリ色（ツールアイコンの地）: CSV加工=青 / 連携=`#7C3AED` / 画像=`#EA580C` / 確認検証=`#16A34A`

### ツールカードの作法

- **ベタ塗りの角丸アイコン**を最上段に置く（カテゴリごとに色が変わる）。インラインSVGで自作し、
  アイコンフォントもCDNも読み込まない
- 説明は1行。2行に収まる量まで
- 対応マーケットプレイスはカード下部に小さく

### ツールページの型（2種類）

**型A: ブラウザで動くツール** — `投入口 → 設定 → 完了` の3状態。
ステップ表示を上に置き、左が設定・右が結果、下に実行ボタン。
**開いた瞬間にファイル投入口が見えていること。** 機能説明を上に積まない（使うと決めて入ってきている）。
使い方・FAQ・関連ツールは投入口の下。

**型B: 説明だけのページ** — 型Aに移行するまでの暫定。極力作らない。

### 記事（AI検索を知る）

**サムネイル / 出典バッジ / 日付 / タイトル / 箇条書き3行** の2列カード。
サムネが用意できない記事は、色と図形だけのSVGを出典ごとの既定の絵として使う（空欄にしない）。

リンク集は腐る。**最初は5本だけ**、それぞれに「なぜ読むべきか」を1行付ける。
各リンクに追加日を入れて、更新されている証拠を見せる。

---

## 実装で踏んだ罠

### `[hidden]` は `display` 指定に負ける（モックアップ3枚で2回踏んだ）

`.card{display:flex}` や `.work{display:grid}` を持つ要素に `hidden` を付けても**消えない**。
CSSの先頭に必ず書く:

```css
[hidden]{display:none!important}
```

絞り込み・タブ・画面の出し分けを作ったら、**書いた直後に機械で確認する**。
目視では「消えていない」ことに気づきにくい。

```bash
node -e "const h=require('fs').readFileSync('ファイル','utf8');
const css=h.match(/<style>([\s\S]*?)<\/style>/)[1];
console.log(/\[hidden\]\s*\{[^}]*display\s*:\s*none/.test(css)?'OK':'NG');"
```

### HTML内の `<script>` は構文チェックされない

`node --check` は `.js` しか見ない。`<script>` を書いたら `vm.Script` に通す。
参照している `id` が実在するかも併せて確認する（打ち間違いは実行するまで分からない）。

### タブを作ったら「空のタブ」が無いか確認する

該当0件のカテゴリタブがあると壊れて見える。タブの `data-cat` とカードの `data-cat` を
突き合わせて、全タブに1件以上あることを機械で確認する。

---

## 未決（次のセッションで確認すること）

1. **実際に配布されているツールの全容。** サポート担当がメールで配っているものが「いくつもある」と
   のこと。現物を集めれば共通の型が見え、ブラウザ移植の可否も判断できる。
   確認済みは2本のみ（後述）
2. **記事のサムネイル方針。** 実画像を用意するか、SVGの既定絵で通すか
3. **パスワード保護の実現手段と、`tools.lisuto.co.jp` の開設。** 方針は決まっているが未着手

---

## 確認済みの実ツール

### 生成AI用 商品説明文ツール（`.py` / Tkinter / 2版）

サポート担当がメールで配布していたもの。**移植可能と判断した。**

- 楽天・Yahoo!の商品CSVを読み、**ChatGPT/Geminiに渡す「指示書TXT」を作るだけ**。
  生成AIのAPIは使っていない（コード冒頭に明記あり）
- 元CSVは書き換えず、出力フォルダにバイト単位でコピーする
- 生成AIには「完成CSV」と「品番別の変更内容一覧CSV」の2つを作らせる
- GUIの項目は4つ: モール / 基本設定（ショップコード・CSV・文字コード）/
  楽天商品属性定義書（**任意**）/ 最終CSVの形（元CSV維持版・説明文更新用）

**2版の差はAIへの指示文だけで、プログラムの作りは同じ**（1677行 vs 1721行）。
通常版は商品ページを見て今ある説明文を整える。WEB情報追加版はメーカー公式なども調べ、
既存文を残したまま新しい事実を足す。

→ **ブラウザ版では2版を1ページに統合し、「調べ方」のラジオ1つで切り替える**設計にした
（モックアップ済み）。ユーザーが「どっちのファイルだったか」を管理しなくてよくなる。

→ 文字コードは実物では手動選択（`Windows：CP932 + surrogateescape`）だったが、
この表記はユーザーに読めない。**自動判別して結果を表示するだけ**に変えた。

**注意: 移植の実装は本気の開発になる。** 指示書を組み立てる部分だけで1000行ほどある。

---

## 成果物の置き方

| フォルダ | 中身 |
|---|---|
| `mockups/` | モックアップHTML。1画面1ファイル |

ファイル名は `YYYY-MM-DD_対象_内容.html`。

現在あるもの（すべて2026-09-11・ダミーデータ）:
- `2026-09-11_トップページ_モックアップ.html` — カテゴリタブ＋ツール8枚＋記事4本
- `2026-09-11_ツール詳細_CSV列エディタ.html` — 型Aの原型
- `2026-09-11_ツール詳細_生成AI商品説明文ツール.html` — 実ツールをブラウザ版として設計したもの

**トップのツール8本は仮置き**で、実在しない。実ツールが揃ったら差し替える。

---

## 矢頭さんとのやりとり

グローバルの `~\.claude\CLAUDE.md` に詳しい。ここでは特に効くものだけ:

- **日本語で書く**（説明・質問・コミットメッセージすべて）
- **推奨を1つ言い切る。**選択肢の羅列を嫌う。ループを何より嫌う
- **まず成果物を出してから分岐を示す。**一度出したものを自分から蒸し返さない
- **前提を疑う役をやる。**決断が速いぶん、前提が誤っているときに気づくのが遅れる
  （「リンクを張らなければ非公開」はこれで止めた）
