# aitagger-tools-site

**AIタッガー利用者向け「AI検索を知る」記事メディアのデータ管理リポジトリ。**

このリポジトリの役割は**記事データ（HTML本文＋メタ情報）の管理のみ**。
表示は **Unbounce**（LISUTO契約済み、矢頭が管理者アカウントを保有）が担う。
ツール（CSV加工・他ツール連携等）はこのリポジトリの対象外（2026-09-16 方針転換）。

`dev\gas-sales-console`（社内向けSales Console）とは**別プロジェクト**。
読み手が社外のお客様であり、顧客情報を置かない規律が正反対なので同居させない。

---

## 方針転換の経緯（2026-09-16）

以前は「ツール」と「記事」を同居させた公開サイト（`tools.lisuto.co.jp`想定）として設計していた。
実装済みツール11本・モックアップも作ったが、**表示はUnbounceに一本化し、この場所は記事データの
管理に専念する**方針に転換した。

- **表示ページ = Unbounce**。矢頭が管理者アカウントを保有し、LISUTOで契約済み
- **このリポジトリ = 記事データの管理のみ**。articles/ の記事HTML・メタ情報・収集/承認フロー（automation/）を扱う
- 実装済みだったツール一式（`tools/`）とツール用モックアップは**削除済み**（矢頭の判断、復元予定なし）
- 旧`index.html`のツール一覧セクションは削除し、**記事一覧のみの簡易ビュー**に縮小した
  （Unbounce連携までの参照用。Unbounceが本番表示になった段階で扱いを再検討する）

**旧方針だったパスワード保護・`tools.lisuto.co.jp`開設・ブラウザ完結ツールの各決定は、
ツールを扱わなくなったため無効。** 本ファイルの以降の記述は記事メディアの運用に絞る。

---

## 最重要: 顧客情報を置かない

このリポジトリの中身は**最終的に社外へ公開される**（Unbounce経由）。

- **企業名・担当者名・金額・ストアコードを、記事本文にも下書きにも置かない**
- 例が要るときは「A社」「sample-shop」のような仮名にする
- スクリーンショットを貼るときは、実データが写っていないか必ず確認する

`gas-sales-console` は顧客情報を扱う前提のリポジトリだが、**ここは逆**。

---

## このリポジトリの構成

| フォルダ/ファイル | 中身 |
|---|---|
| `articles/` | 公開済み記事のHTML本文（1記事1ファイル） |
| `automation/` | 記事の収集・承認フロー（Slack連携スクリプト） |
| `mockups/` | 記事関連の下書き・プレビューHTML |
| `index.html` | 記事一覧の簡易ビュー（Unbounce連携までの参照用） |
| `used-source-urls.md` | 既出の元記事URL一覧（自動生成、下記参照） |

### 記事の型

**サムネイル / 出典バッジ / 日付 / タイトル / 箇条書き3行** が1記事の基本情報。

**トップページ一覧カードの箇条書き（`articles.json`の`bullets`）と、記事詳細ページ
「結論」の箇条書き（HTML内`<ul class="points">`）は別物であり、中身を揃えてはいけない
（2026-09-16判明の事故。「結論」を篇数無制限で拡充した際、その全項目をそのまま
`articles.json`の`bullets`に流用してしまい、一覧カード側が4〜6行になってレイアウトが
崩れた）。**

- **一覧カードの`bullets`（`articles.json`）: 必ず3件、かつ1件あたり全角換算
  55文字以内。** 記事本文を読まなくても概要がつかめる、キャッチーで短い要点3つ。
  「結論」の全項目から重要な3つを抜き出すのではなく、カード表示に適した短い文で
  **別途書き直す**。55文字はUnbounce一覧カード（`unbounce-embed.html`の
  `.am-points`、font-size:12px・line-height:1.65・実効幅約360px）で2行に
  収まる上限であり、**超過した場合は機械的に切り詰めるのではなく、最初から
  収まる文章として書き直す**（2026-09-16判明。件数だけ3件に揃えても、1件が
  長文だと折り返してカードの高さが不揃いになりレイアウトが崩れる）
- **記事詳細ページの「結論」（HTML内`<ul class="points">`）: 篇数無制限。** 詳細は
  下記参照

`automation/build-batch-drafts.js`を使う場合、`articles-batch*.json`に
`bullets`（結論用、篇数無制限）と`cardBullets`（一覧カード用、**必ず3件・
1件55文字以内**）を別フィールドとして持たせる。`cardBullets`の件数または
文字数が条件を満たさない場合、スクリプトが該当箇所を明示してエラーで止まる
（2026-09-16のレイアウト崩れ事故を受けて追加した検証）。

**文体は「である」調（言い切り）で統一する。「です・ます」調は使わない
（2026-09-18判明・明文化）。** 「〜とされています」「〜が求められています」のような
間接的な語尾ではなく、「〜と述べている」「〜が求められる」のように断定で書く。
モデルによって地の文体の癖が異なり、明示しないと語尾が揺れることが判明したため
（sonnet/opus利用時は暗黙に「である」調になっていたが、haiku利用時に「です・ます」調へ
流れた）、モデルを問わず同じ結果になるようここに明文化する。

**「結論」は主観的な言い換えで数値をぼかさない。ページ本文にある実際の数値
（%・件数・期間等）と固有名詞を、要約せずそのまま拾って書く（2026-09-18再徹底）。**
「8項目の機能を提供」のような件数だけの記述で満足せず、その8項目が何であるかを
具体的に列挙する・代表的な数値（削減率、対象店舗数、期間等）を最低1つは本文中から
探して入れる。ページ本文に数値が本当に存在しない発表記事（機能紹介のみ等）の場合は
無理に数値を作らず、その場合のみ数値なしを許容する。

**本文は「背景」「結論」「メッセージ」の3セクション構成で固定する
（2026-09-16、「何が語られているか」「AIタッガーユーザーへの意味」から改称）。**
新規記事・既存記事の書き直し、どちらでもこの3つを必ず満たすこと。

- **背景**: 何が起きたか・何が公開されたか・なぜ公開されたか。その発表が出た経緯・
  位置づけ（前身の機能や過去の発表との関係、業界的な課題認識など）を1〜2文で書く
- **結論**（箇条書き、`<ul class="points">`。**項目数に上限を設けない**）: 何が示されたのか・
  何が変わったのか・重要な数値は何か。**この結論だけを読めば記事本文を読まずとも
  要点が伝わる**ことが基準。「〜を発表した」で終わる単なる要約ではなく、調査記事なら
  ページ本文にある実際の数値（%、件数など）まで拾って書く（推測や自分の言葉での
  言い換えで数値をぼかさない）
- **メッセージ**（`.why`内の1段落）: タグ（属性）・構造化・AIタッガーに**必ずしも
  紐づけなくてよい**。この記事がEC店舗運営者に対して持つ意味・気づきを書く。
  結果としてタグ付けの話に着地するのは良いが、それを前提に書き始めない。
  読み手が「なるほど」と思える具体性を持たせる

CSSクラス名は歴史的経緯で `.background`（背景）/ `.points`（結論の箇条書き）/
`.why`（メッセージのボックス）のまま。**3セクションの本文フォントは
サイズ・色を統一する**（`.background` / `.points li` / `.why p` とも
`font-size:15.5px` 、色は `var(--ink)`）。過去に「背景」だけ薄い色
（`--ink-mid`）・小さいサイズ（15px）になっていた不整合があったため
（2026-09-16修正）、新しいCSSを書くときもこの3つは必ず同じ値に揃える。

一括生成する場合は `automation/build-batch-drafts.js` を使う
（`node automation/build-batch-drafts.js <articles-batch-N.jsonのパス>`）。
このスクリプトのテンプレートに背景・結論・メッセージの出力コードと
フォント統一が実装済みなので、**個別にHTMLを手書きするより優先してこちらを使う**。

**カテゴリは3軸で管理する（2026-09-16、schemaVersion:2で刷新）。** 単一カテゴリ
（旧: AI検索/SEO/楽天AI/Yahoo!AI）は「どこで起きたか」と「読者は何をすべきか」が
混在しており、記事が増えるほど分類に無理が出ていたため分離した。

- `platforms`（配列、複数可）: `google` / `chatgpt` / `rakuten` / `yahoo` / `other-ai` / `amazon`
- `viewpoint`（単一）: `seller`=出店者が今すぐ使える対策 / `buyer`=購入者の行動がどう変わったかという事実
- `theme`（単一）: `visibility`=検索結果・購買導線の変化 / `optimization`=商品情報・タグ付け対策 / `measurement`=計測・分析

Unbounce埋め込み（`unbounce-embed.html`）では`platforms`をタブとして表示する。`theme`は
カードのバッジ色として表示する。**`viewpoint`はデータとしては持つがUIタブには出さない**
（2026-09-16、一度は最上段セグメントとして実装したが撤回）。読者は全員EC運営者＝全員が
「売り手」であり、seller/buyerという人物属性でタブを分けると「自分は売り手なのに買い手
タブを見るべきか」と迷わせる。加えて実データがbuyer視点（プラットフォーム側発表の後追い）に
大きく偏っており、タブとして機能する分布ではなかった。seller視点の記事が十分に増えた段階で
UI復活を再検討する。

`theme`の網羅（特に`optimization`＝出店者が今すぐ使える対策）が構造的に手薄になりがちなため、
Codexへの依頼はテーマごとの本数を数値で明示する（例:「30本、visibility/optimization/measurement
各10本」）。比率ノルマ（「本数◯、うち◯本はこのテーマ」）は水増しを誘発したため撤廃した。

**サムネは必ず元記事のOGP画像（`og:image`）を使う。** `articles/`のHTML（`.thumb`内の`<img src="...">`）と
`articles.json`の`thumbnail`（`{"type":"image","url":"..."}`）の両方に反映する。取得できたのに
`{"type":"default", "category":"..."}`のまま放置しない（過去に取得を怠り3記事がSVG仮アイコンのままになっていた）。
色と図形だけのSVGは、`og:image`が本当に存在しない記事だけの最終手段とする。

### 決定: リサーチはCodexからClaude単体に移行（2026-09-17提起・同日テスト実施・確定）

Codexへのリサーチ依頼（URL選定・出典・要約・分類）は、もともと「Codexの方がリサーチに
強い」という一般的な評判と矢頭さんの直感で採用した経緯があったが、**Claude単独で
リサーチ〜検証〜HTML化〜Slack通知まで通しで行うテストを2026-09-17に実施し、
Codex方式より手戻りが少ないと判断された。以降、記事リサーチはClaude単体で行う。**

Codex方式で発生していた問題（既出URL重複2回連続、サムネイル17/30本不良、記事URL404
複数件）の多くは、Codexのリサーチ能力そのものではなく**実行環境の制約**
（`-s workspace-write`のネットワーク遮断で実在確認・サムネイル取得が構造的に不可能）と
**指示の実行確実性**（「既出URLと照合すること」と明記しても実際には照合し損ねる）に
起因していた。ClaudeはWebSearch/WebFetch/curlを使い、リサーチと検証を同一の文脈内で
一気通貫に行えるため、この種の「申告と実態の乖離」が構造的に起きない。

**Claude単体テスト（2026-09-17）で判明した固有の注意点:**

- **同一発表の後追い記事は、URLの重複チェックだけでは検出できない。** 楽天・Yahoo!の
  説明会・プレスリリースを複数メディアが別々の切り口・別URLで報じており、本文中の
  数値（「70%削減」「41%短縮」等）まで突き合わせて初めて同一発表だと判明した。
  今後もリサーチ時に本文の数値突き合わせ工程を省略しないこと。ただし**単純に同じ
  数値をなぞるだけの後追い記事はNGだが、一次発表を踏まえた上で書き手独自の分析・
  比較・評価が加わっている記事は、テーマを補完する2本目として採用してよい**
  （2026-09-17、矢頭さんの判断）。数値が一致した場合、その後に独自の考察が
  続いているかを追加確認してから除外するかどうかを決める
- **searchengineland.com・perplexity.ai公式ブログ等、Cloudflare配下のドメインは
  curlのHTTPステータスが200/403を不規則に返す（bot対策の誤爆）。** 2026-09-17、
  「curlで403が出た＝ページが実在しない」と誤判定して一次情報に差し替えた事例が
  あったが、矢頭さんが目視確認したところ全件実在する生きたページだった。
  **この種のドメインはcurlでの実在確認をあてにせず、WebFetch（実際にレンダリング
  された内容を読むツール）で本文が読めるかどうかで実在判定する。** 一次情報への
  差し替え自体は悪くない判断だが、「代替できる良い一次情報が無ければ、この種の
  ドメインでもWebFetchで確認した上で採用する」という逃げ道を用意し、本数を
  減らす理由にしない
- **情報源の格付けは「発表元公式か否か」ではなく「検証可能な一次データを含むか」で
  判定する（2026-09-17、矢頭さんの指摘を受けて変更）。** SEOベンダー・コンサルの
  ブログでも、独自データ・独自調査に基づく分析（Ahrefs、Semrush、SparkToro等の
  過去採用実績）は積極的に採用してよい。判定基準は3段階:
  1. 即採用: 独自データ・独自調査に基づく分析（「何件・何%・いつからいつまでの
     調査か」が記事内に明記されている）
  2. 要精査: 数値を引用しているが一次調査元が記事内に明示されていない
     （alhena.aiの「AI引用ページの65%が構造化データを使用」統計が該当。
     この回は不採用としたが、明確な一次情報元の記載があれば採用可）
  3. 却下: 数値的根拠がなく、自社サービスの機能紹介・宣伝が主目的
- **依頼本数は下限として扱い、上限は依頼本数の150%を目安にする
  （2026-09-17、矢頭さんが確定。「30本固定」ではなく「10本依頼なら15本まで、
  20本依頼なら30本まで」という比率で運用する）。** 良質な候補が基準を満たして
  複数見つかった場合は積極的に増やしてよいが、無制限に候補を探し続けて
  本数を膨らませるのもNG。**最もNGなのは、基準を満たす候補が見つからないことを
  理由に依頼本数を下回って提出すること**（この優先順位は変わらない）。
  依頼本数によって「何本の記事をトップページに置きたいか」の意図が異なるため、
  固定本数ではなく率で運用することで、少数依頼にも多数依頼にも同じルールが適用できる
- **重複検出の効率化**: 30本全件を律儀に本文突き合わせるのではなく、まず
  「発表元・発表日・扱う数値」の3点だけを先に一覧化し、この3点が近い組み合わせ
  （同じ発表元・同じ週）だけをWebFetchで本文突き合わせする。全件を深掘りするより
  呼び出し回数を抑えられる

**運用上のメリットの比較**: Codexは非同期で他の作業と並行して投げられる利点があるが、
Claude単体は「Codexの申告を疑って再検証する」二段階フローが丸ごと不要になる分、
総手戻りが少ない。今後の記事リサーチはClaude単体（本ファイル内の該当手順）で行う。
下記「Codexへのリサーチ依頼で必ず守ること」節はCodex運用時の記録として残すが、
新規のリサーチ依頼では使用しない。

### Codexへのリサーチ依頼で必ず守ること（過去記録・現在は不使用。2026-09-16、30本一括依頼の失敗から確立／2026-09-17、サムネイル探索の担当を分離→同日Claude単体移行に伴い運用終了）

Codexへの依頼を作る前に、必ず
[codex-workspace/research/articles/依頼テンプレート.md](../codex-workspace/research/articles/依頼テンプレート.md)
を読んでコピーして使うこと（`codex-workspace`はGitHub管理外のローカルのみのため、
別セッション・別マシンではこのファイル自体にアクセスできない場合がある。その場合は
以下の要点だけでも必ず守ること）。

**0. サムネイル画像URLの探索・記載はCodexに依頼しない（2026-09-17、方針確定）。**
Codexへの依頼はURL・出典・情報源の種類・言語・公開日・プラットフォーム・視点・テーマ・
要約・「なぜ読むべきか」のみとし、**サムネイル画像URLの項目自体を依頼テンプレートから
削除する**。理由は下記1と同じ構造的制約（Codexはページの生HTMLに到達できない）で、
「Codexにまず探させてClaudeが検証・差し替え」という二段階方式は、Codexの試行が
常に100%失敗するため無駄な工程だと判明したため（2026-09-17検証：Codexがfavicon・
404画像・同一画像の使い回しで提出した5件を、Claude自身が対象記事URLに`curl`で
アクセスし`<meta property="og:image">`を`grep`で抜くだけで、5件全件を一発で
正しいog:imageに置き換えられ、200 OKも確認できた）。**サムネイル取得は最初から
Claude自身が担当する工程として設計する**（下記「記事の収集・承認フロー」参照）。

1. **URL実在確認（200 OK）はCodexにやらせない。必ずClaude自身がcurl等で全件行う
   （2026-09-16、原因判明・方針確定）。** `codex exec -s workspace-write`
   のサンドボックスは外向きTCP通信（80/443番ポート含む全ポート）を構造的に遮断しており、
   Codexのシェルコマンドからは`curl: (7) Failed to connect`（HTTPステータス`000`）に
   なることが常に発生する。これはURLの問題でも依頼文の書き方の問題でもなく、
   サンドボックスモード自体のネットワークポリシーであり、セッションや依頼内容に
   関係なく再現する。**Codexは`web_search`ツール（別経路）でフォールバックして
   「確認できた」と報告してくることがあるが、これは実際のHTTP到達性の保証にはならない**
   （以前「Codexが確認した」を信じて14件のサムネイルが403のまま公開されていた事例、
   10件のファイルパスが捏造されていた事例は、いずれもこのフォールバック起因の
   誤認が根本原因だった可能性が高い）
2. **サムネイル画像URLはClaudeが取得する。** 記事URLの生HTMLを`curl`で取得し、
   `<meta property="og:image">`の値を`grep`で抜き、その画像URL自体も200 OK確認する
   （手順は下記「記事の収集・承認フロー」参照）。低解像度版（`width-200`等）が
   見つかった場合は高解像度版が別にあるか確認する。**og:imageが記事固有でなく
   サイト共通のブランドロゴの場合、arXiv等の学術論文サイトのように構造上
   記事固有の画像を持てないサイトに限り例外として許容する**（2026-09-17判断）。
   それ以外（ニュース記事・公式ブログ等で本来記事固有の画像を持てるはずのサイト）で
   共通ロゴ・faviconしか見つからない場合は、本文中の図版を個別に探すか、
   見つからなければその記事自体を候補から外す。**200 OKだけでは足りない。
   画像レスポンスのヘッダーに`Cross-Origin-Resource-Policy: same-site`（または
   `same-origin`）が付いている場合、curlでは200 OKかつ正常なPNG/JPEGとして
   取得できても、ブラウザ上で他サイト（GitHub Pages）に埋め込んで表示すると
   ブロックされ「画像がつぶれる」症状になる（2026-09-17判明。viton13.comの
   記事で発生。`curl -I`にheader行が出る一方、機械的な200確認だけでは
   検知できなかった）。** サムネイル取得時は`curl -s -o /dev/null -w '%{http_code}'`
   だけでなく、`curl -sI`でレスポンスヘッダーも取得し、
   `Cross-Origin-Resource-Policy`が`same-site`/`same-origin`になっていないか
   （`cross-origin`ならOK、ヘッダー自体が無ければ問題なし）を確認する。
   該当する場合、そのドメインの画像はog:imageに限らずサイト内のどの画像を
   使っても同じ制限を受けるため、代替画像を探すのではなく記事自体を候補から外す
3. **鮮度は「本数の少なくとも半数を直近3ヶ月以内」を数値で強制する。** 「直近3ヶ月を
   優先する」という努力目標の書き方では実効性がなく、30本中実質0本しか満たさなかった。
   探す順番も「直近1ヶ月→1〜3ヶ月→3ヶ月〜1年」の順に広げるよう指定し、見つけやすい
   古い記事に先に飛びつく癖を探索順序自体で防ぐ
4. **依頼内で集める記事どうしの重複も、既出URLと同様にチェックする**
   （既出URLとの重複だけでなく、今回の依頼内での重複も別途確認が必要）
5. **既出URL一覧（`used-source-urls.md`）との重複チェックはCodexの申告を信頼しない。
   Claudeが下書きを受け取った時点で必ず機械照合する（2026-09-17判明・方針確定）。**
   依頼文に「既出URLと照合すること」と明記しても、Codexが実際には照合し損ねて
   既出記事をそのまま再提出することが2回連続（第9弾で3件）で発生した。原因を
   依頼文の書き方に求めて表現を強めても再発しており、Codex側の自己申告に
   実行保証がない以上、**Claude側が下書きURL一覧を`used-source-urls.md`の
   全行に対して文字列一致で機械的に突き合わせ、1件でも一致したら理由を問わず
   除外する**工程を、URL実在確認・サムネイル取得と同じタイミングで必ず行う
   （コマンド例は下記「記事の収集・承認フロー」参照）。Codexへの依頼文を
   さらに強く書き直すことでの解決は試みない（過去に試して効果がなかった）

**反映前に必ずClaude自身が機械的な実在確認（curl等でHTTPステータス確認）と
既出URL一覧との機械照合を全件行う。** Codex側の「200でした」「確認できました」
「既出URLと重複していません」という報告はすべて参考情報に留め、鵜呑みにしない。

### Codex CLI (`codex.exe exec`) が別セッションで実行できない場合（2026-09-16判明・解決済み）

Codex連携はVS Code拡張同梱の`codex.exe`（`C:\Users\junic\.vscode\extensions\openai.chatgpt-<version>-win32-x64\bin\windows-x86_64\codex.exe`、`codex login`済み）を
`Bash`ツールから直接呼ぶ方式（本ファイル冒頭の記事収集フロー参照）。

**あるセッションでは`codex.exe exec`が問題なく実行できるのに、別セッション（特に
「自動モード／auto mode」）では「Create Unsafe Agents」ルールで拒否される、ということが
起きた。** 原因はCodex連携の仕組みが壊れたのではなく、**`codex.exe`実行の恒久的な許可
ルールが`~/.claude/settings.json`に存在しなかった**ため。都度の確認プロンプトが出るモード
ではその場で承認できていたが、自動モードは未承認のコマンド実行を安全側で自動拒否するため、
確認プロンプト自体が発生せずブロックされた。

**`~/.claude/settings.json`に許可ルールを追加済み（矢頭さんが手動で追記。以下は残しておく）:**
```json
"permissions": {
  "allow": [
    "Bash(*/openai.chatgpt-*/bin/windows-x86_64/codex.exe exec *)",
    "Bash(*/openai.chatgpt-*/bin/windows-x86_64/codex.exe login *)"
  ]
}
```

**重要: この許可ルールの追加はClaude自身では絶対に行えない。** 自動モードには
「Self-Modification（自己書き換え）防止」の安全装置があり、自分の権限設定ファイルを
自分で書き換える操作は、どんな正当な理由を提示しても一律拒否される（設計上意図的な
制約であり、バグでも回避すべき対象でもない）。将来また同様のブロックに遭遇した場合、
`settings.json`の編集自体は矢頭さんご本人に依頼する必要がある。

**ただしこのルールだけでは自動モード（auto mode）の別レイヤーのブロックは解決しなかった
（2026-09-16判明）。** `permissions.allow`を追加した別セッションでも、自動モードの分類器
（classifier）は「Create Unsafe Agents → Auto-Mode Bypass → Blocked by classifier」と
理由を変えながら`codex.exe exec`の実行を拒否し続けた。分類器はコマンド名そのものではなく
**「境界の曖昧なコマンドの組み立て方」を「見慣れない・境界の広い操作」として警戒していた**
というのが実際の原因であることが分かった。

**恒久対処法（実際に通った組み立て方。必ずこの形で呼ぶこと）:**
```bash
CODEX="/c/Users/junic/.vscode/extensions/openai.chatgpt-<version>-win32-x64/bin/windows-x86_64/codex.exe"
"$CODEX" exec -C "C:\Users\junic\dev\codex-workspace" -s workspace-write "research/articles/<依頼ファイル>.md を読み、その指示に従って...してください。" 2>&1
```

- **`cd`で移動してから実行しない。** 必ず`-C <作業ディレクトリ>`フラグで明示的に渡す
- **`-s workspace-write`を必ず明示する**（サンドボックスモードを省略しない）
- **依頼文はプロンプト引数にベタ書きせず、「ファイルを読ませて、その指示に従わせる」形にする**
  （依頼内容自体は`research/articles/*.md`ファイルとして事前に書いておき、プロンプトは
  「このファイルを読んで実行して」という短い指示文にする）
- **`2>&1`でリダイレクトし、バックグラウンド実行にする**

この形式であれば、自動モードのセッションでもブロックされずに実行できた（2026-09-16、
動作確認用記事1本のリサーチ→HTML化→draft push→Slack通知まで通しで成功）。

**ただし`-s workspace-write`は外向きTCP通信（80/443番ポート含む全ポート）を構造的に
遮断しており、Codex側からのURL到達確認（curl等）は常に失敗する（2026-09-16判明、
詳細は上記「Codexへのリサーチ依頼で必ず守ること」1番を参照）。** これは実行の組み立て方や
セッションの違いによるものではなく、サンドボックスモードそのものの制約。
`-s danger-full-access`にすれば制約は外れるが、承認なしで任意コマンドを無制限実行できる
状態になりリスクが高いため使わない方針とした（2026-09-16決定）。URL実在確認は
Codexに担わせず、常にClaude側が別途curlで行う。

リンク集は腐る。**厳選して少数を保つ**。各記事に「なぜ読むべきか」が伝わる箇条書きを付け、
公開日を入れて更新されている証拠を見せる。

### 記事の収集・承認フロー

**矢頭さんは「番号リストのテキスト」ではなく「実際に描画されたページ」を見てから承認・否認したい、
という要望（2026-09-16確認）に基づき、承認前にHTML化してGitHub Pages上のdraft URLとして
見せる方式にしている。** Slack Incoming Webhookは投稿専用で矢頭さんの返信を受信できないため、
承認の意思表示は必ずClaude Codeのチャットで直接行う（Slack上で返信しても検知されない）。

```
1. Codex: 元記事を調査し、下書きMarkdownに背景・結論（元の数値・固有名詞を含む箇条書き、
           篇数無制限）を書く（「メッセージ」パートとサムネイル画像URLはここでは書かない。
           サムネイル探索・URL実在確認もCodexにはやらせない＝上記の通り構造的に不可能）
2. Claude: **まず既出URL一覧との機械照合を行う（2026-09-17追加。最優先で行う）。**
           Codexの下書きに書かれた記事URLをすべて抜き出し、`used-source-urls.md`の
           全行と文字列一致（ドメイン＋パス単位）で突き合わせる。1件でも一致したら
           Codexの「既出URLと重複していません」という申告を無視し、理由を問わず除外する
           （例: bashで一覧をループしurlをgrepするなど、目視でなく必ずコマンドで行う）。
           次に残った記事URLを全件curlで機械確認する（200 OK確認。Codexの「確認できた」
           報告は鵜呑みにしない）。続けて各記事URLの生HTMLをcurlで取得し、
           `<meta property="og:image">`をgrepで抜いてサムネイル画像URLを取得し、
           その画像URL自体も200 OKかを確認する（2026-09-17検証：この方式でCodexが
           favicon・404画像に逃げた5件を一発で正しい画像に置き換えられた）。
           og:imageが無い・404の場合のみ、本文中の画像を個別に探す
3. Claude: 「メッセージ」パートを書き足し、記事HTMLを articles/draft/<slug>.html に生成
           （背景/結論/メッセージの3セクション構成、カード用bulletsは3件・55文字以内）。
           対応するメタ情報（articles.json登録用の1件分）を automation/draft-meta/<slug>.json に保存。
           git push（articles/draft/ 配下はarticles.json未登録のため一覧・Unbounceには一切出ない）
4. Claude: Slack通知（下書きURL付き）を投稿
     node automation/post-articles-to-slack.js <slug> <slug> ...
5. 矢頭さん: 通知内のGitHub Pages URL（articles/draft/<slug>.html）で実際のページを確認し、
           Claude Codeのチャットで「1と3を承認」のように直接伝える
6a. 承認: node automation/promote-draft.js <slug> <slug> ...
     → articles/draft/ から articles/ 直下へ移動、articles.jsonに登録、
       used-source-urls.md再生成まで一括実行。その後 git push。
     （promote前にarticles.json用のbullets＝一覧カード用3件・55文字以内を、
       draft-metaのbulletsとして別途用意しておくこと。結論の全項目をそのまま
       流用するとカードのレイアウトが崩れる。詳細は「記事の型」節を参照）
6b. 否認: node automation/reject-draft.js <slug> <slug> ...
     → draftのファイルを削除するだけ。articles.json未登録なので実害なし。
```

**承認push後、Unbounce側のトップページに反映されるまで数十秒のタイムラグがある**
（2026-09-16確認。GitHub Pages・articles.jsonは即時反映されているが、Unbounce側のCDN/ページ
キャッシュ由来と見られる遅延がある）。すぐ表示されなくても異常ではないので、数十秒〜数分待って
から再確認する。

`automation/config.local.json`（gitignore対象）にSlack Webhook URLを置く。
`config.local.json.example` がテンプレート。

**既出の元記事URLは、Codexへのリサーチ依頼前に`used-source-urls.md`で除外する**（下記参照）。
promote-draft.js実行時にも自動再生成されるため、都度手動実行し忘れても次の昇格時に追いつく。

### 重複記事の防止（Codexへの依頼時点で除外する）

**テーマがかぶるのは問題ないが、同じ元記事URLの再利用はNG。** 出来上がってからSlackで弾かれるのでは
遅い。**記事候補を探す作業をCodexに依頼する前に、必ず`used-source-urls.md`を読み込ませ、
そこに載っているURLと同じ元記事は調査対象から外すよう指示すること。**

`used-source-urls.md`は`articles.json`の`sourceUrl`一覧から生成される。
`articles.json`に記事を追加したら、都度これを実行して最新化する。

```
node automation/generate-used-urls.js
```

---

## 色（記事メディアとして使うもの）

AIタッガー管理画面の青系トーンを踏襲する。新しい色を増やさない。

| 用途 | 値 |
|---|---|
| アクセント | `#2563EB` |
| 見出し | `#1E3A8A` |
| 本文 | `#0F172A` / 補助 `#475569` / 薄 `#64748B` |
| 罫線 | `#E2E8F0` / 面の色 `#F8FAFC` |

テーマバッジ色: 検索結果・購買導線の変化(visibility)=青`#2563EB` / 商品情報・タグ付け対策(optimization)=紫`#7C3AED` / 計測・分析(measurement)=赤`#DC2626`

---

## 実装で踏んだ罠（記事HTML・簡易ビューに引き続き有効）

### `[hidden]` は `display` 指定に負ける

`display:flex` や `display:grid` を持つ要素に `hidden` を付けても**消えない**。
CSSの先頭に必ず書く:

```css
[hidden]{display:none!important}
```

絞り込み・タブ・画面の出し分けを作ったら、**書いた直後に機械で確認する**。
目視では「消えていない」ことに気づきにくい。

```bash
node -e "const h=require('fs').readFileSync('ファイル','utf8');
const css=h.match(/<style>([\s\S]*?)<\/style>/)[1];
console.log(/\[hidden\]\s*\{[^}]*display\s*:\s*none/.test(css)?'OK':'NG');"
```

### HTML内の `<script>` は構文チェックされない

`node --check` は `.js` しか見ない。`<script>` を書いたら `vm.Script` に通す。
参照している `id` が実在するかも併せて確認する（打ち間違いは実行するまで分からない）。

### 記事HTMLの必須セクション（背景・何が語られているか・AIタッガーユーザーへの意味）はテンプレート側で担保する

2026-09-16、30本一括生成した記事から「背景」セクション（`<h2>背景</h2><p class="background">...</p>`、
タイトル直後・「何が語られているか」の前に入る）が全件（本番31本・draft30本）欠落した事故があった。
原因は `automation/build-batch-drafts.js`（一括生成スクリプト）のHTML出力コードに、そもそも
「背景」を出力する行が存在しなかったこと。CSS（`.background`）は定義されていたため見た目のバグに
気づきにくく、個別に手作業で作っていた記事（`yahoo-ai-shopping-memo.html`等）には残っていたため
発見が遅れた。

**再発防止として`build-batch-drafts.js`に以下を実装済み:**

- `background` を含む必須フィールド（`REQUIRED_FIELDS`）が `articles-batch*.json` の各要素に
  存在するか `validate()` で検証し、欠落があれば生成前に例外で止める
- 新しい記事セクション（見出し）をテンプレートに追加する場合も、同様に必須フィールド化し
  `validate()` に追加すること。CSSだけ定義して出力コードを書き忘れる、という同型のミスを防ぐため

**一括生成スクリプトを新規に作る・複製するとき:**

- 出力するHTMLの構造を、既存の完成記事（`articles/*.html`）と実際に diff で見比べる
  （見出しの数・順序が一致するか）。「動いたから正しい」ではなく「既存記事と同じ構造か」で確認する
- 生成直後に `grep -c "<h2>" 生成物.html` などで見出し数を機械カウントし、既存記事の見出し数と
  一致するか確認してから大量投入する

---

## Unbounce連携（次のステップ）

準備手順はチャットで都度確認する（本ファイルには最新の決定事項のみ反映していく）。
連携が固まり次第、ここに手順・API/エクスポート方式・記事データの受け渡しフォーマットを追記する。

---

## 矢頭さんとのやりとり

グローバルの `~\.claude\CLAUDE.md` に詳しい。ここでは特に効くものだけ:

- **日本語で書く**（説明・質問・コミットメッセージすべて）
- **推奨を1つ言い切る。**選択肢の羅列を嫌う。ループを何より嫌う
- **まず成果物を出してから分岐を示す。**一度出したものを自分から蒸し返さない
- **前提を疑う役をやる。**決断が速いぶん、前提が誤っているときに気づくのが遅れる
